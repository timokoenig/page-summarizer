# Use a local model path to avoid downloads at runtime (recommended after snapshot):
# HF_LOCAL_PATH=./models/distilbart-cnn-12-6

# Or use a model id (downloads on first use):
HF_MODEL_ID=sshleifer/distilbart-cnn-12-6

# Summary length target in words (the code maps to tokens)
SUMMARY_TARGET_WORDS=140

# Extraction: max chars to pass to summarizer (keeps latency bounded)
EXTRACT_MAX_CHARS=20000

# Trafilatura recall (0 = precision, 1 = recall)
TRAFILATURA_FAVOR_RECALL=0